# Frontend Web (Giai đoạn Phục vụ - Serving)

Thư mục này chứa code giao diện người dùng.

### Các việc cần làm (To-Do):
1. Tạo \pp.py\: Dùng framework \streamlit\ dựng giao diện Web.
2. Tạo thanh Dropdown chọn mã chứng khoán, thanh chọn Ngày/Tháng.
3. Dùng thư viện \equests\ gọi HTTP GET lên đường link URL của **API Gateway**.
4. Nhận cục JSON trả về, đưa vào \plotly\ hoặc \st.line_chart\ để vẽ biểu đồ nến.
5. Tạo \equirements.txt\. Chạy local bằng lệnh: \streamlit run app.py\.
